import axios from "axios";

const fetchExtract = async (start, end, type) => {
    baseURL: `https://devatlas.cloud/app/extract/{}`
}
const extratoApi = axios.create({
    baseURL: "https://devatlas.cloud/app/extract/"
})

export default extratoApi